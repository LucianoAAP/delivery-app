import React from 'react';
import { CustomerHeader, ProductList } from '../components';

const CustomerPage = () => (
  <main>
    <CustomerHeader />
    <ProductList />
  </main>
);

export default CustomerPage;
